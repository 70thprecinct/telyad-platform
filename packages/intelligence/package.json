{
  "name": "@telyad/intelligence",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic demonstration intelligence (planner, creative, audience, revenue, localisation) behind swappable interfaces",
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@telyad/ad-formats": "workspace:*",
    "@telyad/audience": "workspace:*",
    "@telyad/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
